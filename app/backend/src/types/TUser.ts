export type TUserValidate = {
  user: {
    id: number;
    username: string;
    role: string;
    email: number;
  },
};

export type TUserModel = {
  id: number;
  username: string;
  role: string;
  email: string;
};
